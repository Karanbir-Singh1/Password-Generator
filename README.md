# Password-Generator
In this project, I built a secure password generator in Java using SecureRandom. The program allows the user to choose:  Password length  Whether to include uppercase, lowercase, digits, and symbols.  Whether to avoid ambiguous characters like I, l, 1, O, 0
